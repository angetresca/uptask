extends layout

include mixins/newProject

block contenido
    .contenido-principal
        h1 #{pageName}
        if errors
            each error in errors
                .alerta #{error.text}

        +newProject(project)  # This is inside the mixin

        