<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Inicia sesión en Uptastk

            form#fomulario.caja.caja-login(method="POST" action="login")
                .campo
                    label(for="email") Email:
                    input(type="email" name="email" placeholder="Tu email", value= email)
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" placeholder="Tu contraseña" value= password)
                .campo.enviar
                    input.boton(type="submit", value="Iniciar sesión")
                .campo
                    a(href="/login") Aún no tienes cuenta? Crea una gratis.
