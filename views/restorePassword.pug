<!DOCTYPE html>
html(lang='en')
    head
        include partials/header
    body.login 
        .contenedor-formulario
            h1= pageName

            include partials/errors

            p Agrega tu email para reestablecer tu contraseña
            form.caja-login(method="POST" action="/restore-password")
                .campo
                    label(for="email") Email:
                    input(type="email" name="email" placeholder="Tu email", value= email)
                .campo.enviar   
                    input.boton(type="submit", value="Reestablecer contraseña")
                .campo.acciones
                    a(href="/login") Volver al login